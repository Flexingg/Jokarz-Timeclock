*.iml
.gradle
/local.properties
/.idea
.DS_Store
build/
*/build/
captures/
.externalNativeBuild
.cxx
gradle-dist/
android-build/
.kotlin/

# Signing material must never be committed (the password file is as sensitive as the keystore).
keystore.properties
keystore/
*.jks
*.keystore

# Kotlin build session state
.kotlin/
